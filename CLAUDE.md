# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

`README.md` is the source of truth for setup, the build and update
commands, the seed vs post-seed architecture, the share topology,
the host-side publish recipe, and troubleshooting. Read it first
and work from it directly — do not restate its content here.
